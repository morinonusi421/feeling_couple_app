<% provide(:title, 'パーティの作成') %>
<h1>パーティの作成ページ</h1>


<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(model: @party) do |f| %>
      <%= f.label :name, "パーティの名前" %>
      <%= f.text_field :name %>
      <%= f.submit "パーティを作成する！", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>